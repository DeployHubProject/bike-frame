apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bike-frame.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "bike-frame.name" . }}
    helm.sh/chart: {{ include "bike-frame.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bike-frame.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
